pipeline {
    agent any
    stages {
        stage {
            when {
                expression {
                    Network == 'True'
                }
        steps {
            echo "stage is succesfully deployed"
        }
        }
    }
}
}