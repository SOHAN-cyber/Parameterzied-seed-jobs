pipeline {
    agent any
         properties([
                     parameters([
                         booleanParam(
                              defaultValue: true, 
                              description: 'Do you want to create Network', 
                              name: 'Network'
                        )
                     ])
                  ])
    stages {
        stage {
            when {
                expression {
                    Network == 'True'
                }
        steps {
            echo "stage is succesfully deployed"
        }
        }
    }
}   
}